# The core engine (feasibility/) is pure Python standard library — no runtime deps.
# These are dev/test/benchmark extras only.
pytest>=8.0
matplotlib>=3.5   # used only by benchmarks/ and docs/make_architecture.py
